class Goal {
  String id;
  DateTime deadline;
  String name;
  List<String> tasksReference;
  String ownerID;
  String username;

  Goal(this.id, this.deadline, this.name, this.tasksReference, this.ownerID,
      this.username);
}
